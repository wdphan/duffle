import React from 'react';
import { CustomTheme, Mode, Theme } from '../../types';
declare type ConnectButtonRendererProps = {
    children?: (renderProps: {
        show?: () => void;
        hide?: () => void;
        unsupported: boolean;
        isConnected: boolean;
        isConnecting: boolean;
        address?: string;
        truncatedAddress?: string;
        ensName?: string;
    }) => React.ReactNode;
};
declare type ConnectKitButtonProps = {
    label?: string;
    showBalance?: boolean;
    showAvatar?: boolean;
    theme?: Theme;
    mode?: Mode;
    customTheme?: CustomTheme;
    onClick?: (open: () => void) => void;
};
export declare function ConnectKitButton({ label, showBalance, showAvatar, theme, mode, customTheme, onClick, }: ConnectKitButtonProps): JSX.Element;
export declare namespace ConnectKitButton {
    var Custom: React.FC<ConnectButtonRendererProps>;
}
export {};
