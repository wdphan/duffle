import { Chain, Connector } from 'wagmi';
import { Provider } from '@wagmi/core';
export declare const getAppName: () => string;
declare type DefaultClientProps = {
    appName: string;
    autoConnect?: boolean;
    alchemyId?: string;
    infuraId?: string;
    chains?: Chain[];
    connectors?: any;
    provider?: any;
};
declare const defaultClient: ({ autoConnect, appName, chains, alchemyId, infuraId, connectors, provider, }: DefaultClientProps) => {
    autoConnect?: boolean;
    connectors?: Connector[];
    provider: Provider;
    webSocketProvider?: any;
};
export default defaultClient;
